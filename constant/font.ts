import { Questrial, Rozha_One } from '@next/font/google';

export const header = Rozha_One({
	weight: '400',
	subsets: ['latin'],
});

export const paragraph = Questrial({
	weight: '400',
	subsets: ['latin'],
});
