import React from 'react';

import { CardHeader, CardBody, Card } from './index.types';

export const NeedsReview = () => {
	return (
		<Card className="bg-blue-500">
			<CardHeader>AllocatedTaskMembers</CardHeader>
			<CardBody></CardBody>
		</Card>
	);
};
